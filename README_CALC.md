# Calculator

`calculator.py` exposes two pure functions:

- `add(left, right)` returns the sum.
- `subtract(left, right)` returns the difference.

Both functions accept numeric values and return a numeric result. Example: `add(2, 3) == 5`.
